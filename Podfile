# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'finManager' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for finManager
  pod 'Plaid', '~> 3.0.0'

  target 'finManagerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'finManagerUITests' do
    # Pods for testing
  end
end
